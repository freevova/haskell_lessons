-- Q10.2 Write a threeRoundFight function that takes two robots and has them fight
--   for three rounds, returning the winner. To avoid having so many different variables for robot state,
--   use a series of nested lambda functions so you can just overwrite robotA and robotB.

